import React from 'react'

export default function PoolingTab(){
  return (
    <div>
      <h2 className="text-xl font-semibold mb-2">Pooling</h2>
      <p>Pooling UI placeholder — use backend endpoints to load adjusted CB and create pools.</p>
    </div>
  )
}
